import styled from "styled-components";

const STYLE = {
  Main: styled.div`
    height: 100%;
    position: relative;
    overflow-y: scroll;
    scroll-snap-type: y mandatory;
  `,
};

export default STYLE;
